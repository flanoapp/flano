import React from 'react';
import { StyleSheet, Text } from 'react-native';

const SplashScreen = () => {
    return <Text style={{ fontSize: 48 }}> SplashScreen </Text>;
};

const styles = StyleSheet.create({});

export default SplashScreen;
